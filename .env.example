# Frontend Environment Variables for Quiz Master Live
# Copy this file to .env and fill in your actual values

# Supabase Configuration (required)
# Get these values from your Supabase project dashboard at https://supabase.com/dashboard

# Your Supabase project URL (found in Settings > API)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (found in Settings > API)
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Optional: Backend API (if you have a separate backend server)
# VITE_API_URL=http://localhost:5000/api
# VITE_WS_URL=http://localhost:5000

# ⚠️  SECURITY WARNING ⚠️
# Never include backend secrets in frontend environment variables:
# - SUPABASE_SERVICE_ROLE_KEY (backend only)
# - JWT_SECRET (backend only)
# - DATABASE_URL (backend only)
# - Any secrets or private keys (backend only)
#
# Frontend environment variables are exposed to the browser and publicly visible!</parameter>